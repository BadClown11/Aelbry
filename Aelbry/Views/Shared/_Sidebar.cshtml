<aside class="sidebar" id="sidebar">
    <div class="sidebar__project-picker" id="projectPicker">
        <label class="sidebar__section-label sidebar-label">Proyecto Activo</label>
        <select class="project-select" id="activeProjectSelect" onchange="onActiveProjectChange()">
            <option value="">— Seleccionar proyecto —</option>
        </select>
    </div>
    <nav class="sidebar__nav" id="sidebar-nav">

        <a href="/Home/Index" class="nav-item" data-view="home">
            <span class="nav-item__inner">
                <i data-lucide="home" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Inicio</span>
            </span>
        </a>

        <span class="sidebar__section-label sidebar-label">Administración</span>

        <a href="/Company/Index" class="nav-item" data-view="company" data-permission="COMPANY_MANAGE">
            <span class="nav-item__inner">
                <i data-lucide="building-2" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Empresas</span>
            </span>
        </a>

        <a href="/User/Index" class="nav-item" data-view="users" data-permission="USERS_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="users" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Usuarios</span>
            </span>
        </a>

        <a href="/Department/Index" class="nav-item" data-view="departments" data-permission="DEPARTMENTS_MANAGE">
            <span class="nav-item__inner">
                <i data-lucide="network" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Departamentos y Equipos</span>
            </span>
        </a>

        <a href="/Project/Index" class="nav-item" data-view="projects" data-permission="PROJECTS_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="folder-kanban" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Proyectos</span>
            </span>
        </a>

        <span class="sidebar__section-label sidebar-label">Vistas de Proyecto</span>

        <a href="/Activity/Index" class="nav-item" data-view="activities" data-permission="ACTIVITIES_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="list-todo" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Actividades</span>
            </span>
        </a>

        <a href="/Kanban/Index" class="nav-item" data-view="kanban" data-permission="ACTIVITIES_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="kanban" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Tablero Kanban</span>
            </span>
            <span class="nav-badge nav-badge--neutral sidebar-badge" id="badge-kanban">6</span>
        </a>

        <a href="/Gantt/Index" class="nav-item" data-view="gantt" data-permission="ACTIVITIES_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="gantt-chart" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Gantt &amp; CPM</span>
            </span>
        </a>

        <a href="/CalendarView/Index" class="nav-item" data-view="calendar" data-permission="ACTIVITIES_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="calendar" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Calendario</span>
            </span>
        </a>

        <span class="sidebar__section-label sidebar-label">Colaboración</span>

        <a href="/TimeEntry/Index" class="nav-item" data-view="timeentry" data-permission="TIME_ENTRIES_MANAGE">
            <span class="nav-item__inner">
                <i data-lucide="timer" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Mis Horas</span>
            </span>
        </a>

        <a href="/Chat/Index" class="nav-item" data-view="chat" data-permission="CHAT_USE">
            <span class="nav-item__inner">
                <i data-lucide="message-square-code" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Chat del Proyecto</span>
            </span>
            <span class="nav-badge nav-badge--danger sidebar-badge" id="badge-chat">3</span>
        </a>

        <a href="/Document/Index" class="nav-item" data-view="documents" data-permission="DOCUMENTS_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="files" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Documentos</span>
            </span>
        </a>
        <span class="sidebar__section-label sidebar-label">Herramientas</span>

        <a href="/AiAssistant/Index" class="nav-item" data-view="ai" data-permission="AI_ASSISTANT_USE">
            <span class="nav-item__inner">
                <i data-lucide="sparkles" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Asistente IA</span>
            </span>
        </a>

        <a href="/AutomationRule/Index" class="nav-item" data-view="automations" data-permission="AUTOMATION_RULES_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="zap" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Automatizaciones</span>
            </span>
        </a>

        <a href="/Report/Index" class="nav-item" data-view="reports" data-permission="REPORTS_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="bar-chart-2" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Reportes</span>
            </span>
        </a>

        <a href="/AuditLog/Index" class="nav-item" data-view="audit" data-permission="AUDIT_VIEW">
            <span class="nav-item__inner">
                <i data-lucide="shield-check" class="nav-item__icon"></i>
                <span class="nav-item__label nav-label">Auditoría</span>
            </span>
        </a>

    </nav>
    <div class="sidebar__footer">
        <a href="/AiAssistant/Index" class="btn-ai" id="btn-ai" data-permission="AI_ASSISTANT_USE">
            <i data-lucide="sparkles"></i>
            <span class="nav-label">Asistente IA (Gemini)</span>
        </a>

        <button class="btn-collapse" onclick="toggleSidebar()" id="btn-collapse">
            <i data-lucide="chevron-left" id="collapse-icon"></i>
            <span class="nav-label" id="collapse-text">Colapsar Menú</span>
        </button>
    </div>
</aside>

<script>
    // ── Marcar enlace activo según URL actual ──
    function setActiveLink() {
        const path = window.location.pathname.toLowerCase();
        document.querySelectorAll('.nav-item[data-view]').forEach(link => {
            const href = (link.getAttribute('href') || '').toLowerCase();
            // Coincidencia por controlador (primer segmento)
            const controller = href.split('/')[1];
            if (controller && path.includes(controller)) {
                link.classList.add('nav-item--active');
                const icon = link.querySelector('.nav-item__icon');
                if (icon) icon.classList.add('nav-item__icon--active');
            }
        });
    }
    setActiveLink();

    // ── Ocultar opciones que el usuario logueado no tiene permiso de usar ──
    // (la validacion real sigue viviendo en el servidor via [Authorize(Policy)]; esto
    // solo evita mostrar enlaces que terminarian en un 403).
    function filterByPermissions() {
        const permissions = window.Aelbry?.api?.getPermissions?.() ?? [];

        document.querySelectorAll('#sidebar-nav [data-permission], #btn-ai[data-permission]').forEach((el) => {
            if (!permissions.includes(el.getAttribute('data-permission'))) {
                el.style.display = 'none';
            }
        });

        // Ocultar tambien el titulo de una seccion si todos sus items quedaron ocultos.
        document.querySelectorAll('#sidebar-nav .sidebar__section-label').forEach((label) => {
            let sibling = label.nextElementSibling;
            let anyVisible = false;

            while (sibling && !sibling.classList.contains('sidebar__section-label')) {
                if (sibling.style.display !== 'none') anyVisible = true;
                sibling = sibling.nextElementSibling;
            }

            label.style.display = anyVisible ? '' : 'none';
        });
    }

    // ── Portal de entrada: Admin siempre ve el sidebar completo; el resto de roles
    // primero cae en Inicio (solo topbar) y hasta que elige un proyecto (desde una fila
    // del feed de actividades) se revela el sidebar para trabajar en ese proyecto. Inicio
    // siempre oculta el sidebar de nuevo, sin importar si ya hay un proyecto activo. ──
    function isHomePage() {
        return window.location.pathname.toLowerCase().startsWith('/home');
    }

    function applyProjectGate() {
        const user = window.Aelbry?.api?.getCurrentUser?.();
        const isAdmin = !!user?.roles?.includes('Admin');
        const activeProjectId = window.Aelbry?.projectContext?.getId?.();
        const activeProjectName = window.Aelbry?.projectContext?.getName?.();
        const sidebar = document.getElementById('sidebar');
        const projectPicker = document.getElementById('projectPicker');

        // El Admin ve toda la empresa de una vez, no un proyecto a la vez: el selector
        // "Proyecto Activo" no le sirve de nada, asi que ni se muestra.
        projectPicker.style.display = isAdmin ? 'none' : '';

        if (!isAdmin && (isHomePage() || !activeProjectId)) {
            sidebar.style.display = 'none';
            return;
        }

        sidebar.style.display = '';

        const select = document.getElementById('activeProjectSelect');
        if (activeProjectId && select && !select.querySelector(`option[value="${activeProjectId}"]`)) {
            const opt = document.createElement('option');
            opt.value = activeProjectId;
            opt.textContent = activeProjectName || `Proyecto #${activeProjectId}`;
            select.appendChild(opt);
        }
        if (select && activeProjectId) {
            select.value = activeProjectId;
        }
    }

    function onActiveProjectChange() {
        const select = document.getElementById('activeProjectSelect');
        if (!select.value) {
            window.Aelbry?.projectContext?.clear?.();
            window.location.href = '/Home/Index';
        }
    }

    // api.js se carga mas abajo en el layout (despues de este partial), asi que hay que
    // esperar a que el documento termine de parsear/ejecutar todos los <script> antes de leerlo.
    document.addEventListener('DOMContentLoaded', () => {
        filterByPermissions();
        applyProjectGate();
    });

    // ── Colapsar sidebar ──
    let sidebarCollapsed = false;

    function toggleSidebar() {
        sidebarCollapsed = !sidebarCollapsed;
        const sidebar = document.getElementById('sidebar');
        const collapseIcon = document.getElementById('collapse-icon');

        sidebar.classList.toggle('sidebar--collapsed', sidebarCollapsed);
        collapseIcon.setAttribute('data-lucide', sidebarCollapsed ? 'chevron-right' : 'chevron-left');
        lucide.createIcons();

        // Notificar al layout padre si existe (para ajustar el main)
        window.dispatchEvent(new CustomEvent('sidebarToggle', { detail: { collapsed: sidebarCollapsed } }));
    }
</script>
